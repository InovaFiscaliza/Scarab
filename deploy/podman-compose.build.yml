services:
  db:
    build:
      context: "${SCARAB_SOURCE_DIR:?Set SCARAB_SOURCE_DIR to the source checkout}"
      dockerfile: deploy/Containerfile.db

  app:
    build:
      context: "${SCARAB_SOURCE_DIR:?Set SCARAB_SOURCE_DIR to the source checkout}"
      dockerfile: deploy/Containerfile.app